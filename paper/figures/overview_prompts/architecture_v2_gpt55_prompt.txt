Create a 1024×1024 pixel publication-quality NeurIPS-style vector-flat scientific architecture figure on a pure white background, using a radial/concentric dataflow composition rather than a flat horizontal pipeline. Show MC-WM as a sim-to-real MBRL system with a large central rounded hexagon labeled “corrected model M_sim + δ_θ”. Above the entire diagram, place a wide pale-gray rounded rectangle labeled “Claude/GLM LLM orchestrator”, with four small role chips beneath it labeled “#1 constraints”, “#2 features”, “#3 audit”, and “#5 HP-Pareto gate”; draw thin dashed control arrows from #1 to the constraint mask, #2 to the residual model, #3 to the Bellman trust gate, and #5 to RE-SAC. On the upper-left arc, draw a sim branch: “sim env M_sim” feeding “sim data τ_sim” feeding “ensemble world model M_sim”, then into the central corrected model. On the lower-left arc, draw a real branch: “real env M_real” feeding “50k real transitions τ_real” feeding “residual δ_θ = SINDy basis Φ(s,a) + NAU/NMU”, then into the central corrected model. On the right arc, draw “imagined rollouts τ” flowing from the central model into two sequential filter gates labeled “Bellman Q^δ trust weight β” and “constraint mask m”, with a small attached tag “discount γ” on the Bellman gate; the output is a small card labeled “surviving transitions” flowing downward into “RE-SAC”. At the bottom, show “RE-SAC” updating “SAC policy π”, with a curved feedback arrow from “SAC policy π” back to “real env M_real”. Use solid arrows for data/model flow and dashed arrows for LLM orchestration, with clear arrowheads and no crossing where possible. Use only this palette for shapes and strokes: deep teal #264653, muted orange #E76F51, sand #F4A261, olive #8DB580, pale gray #E9ECEF; use charcoal #2b2b2b for all text. Use Helvetica-like sans-serif labels, high legibility, consistent spacing, flat fills, thin clean outlines, no gradients. Use real glyphs exactly for δ, β, π, τ, γ and never transliterate them. The only text allowed in the image is exactly: “MC-WM ARCHITECTURE”, “Claude/GLM LLM orchestrator”, “#1 constraints”, “#2 features”, “#3 audit”, “#5 HP-Pareto gate”, “sim env M_sim”, “sim data τ_sim”, “ensemble world model M_sim”, “real env M_real”, “50k real transitions τ_real”, “residual δ_θ = SINDy basis Φ(s,a) + NAU/NMU”, “corrected model M_sim + δ_θ”, “imagined rollouts τ”, “Bellman Q^δ trust weight β”, “discount γ”, “constraint mask m”, “surviving transitions”, “RE-SAC”, and “SAC policy π”. No extra labels, no legend, no axes, no panel letters, no logos, no watermarks, no 3D, no shadows, no photorealism, and no cartoons.
