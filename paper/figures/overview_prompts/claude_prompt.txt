Create a single horizontal 16:9 overview figure for a NeurIPS/ICLR paper, pure vector-flat scientific style, white background, thin 0.6pt black outlines, sans-serif labels (Helvetica-like), charcoal #2b2b2b body text, palette strictly limited to deep teal #264653, muted orange #E76F51, sand #F4A261, olive #8DB580, pale gray #E9ECEF fills — no gradients, no 3D, no shadows, no photorealism, no cartoon, no corporate logos, no watermarks. Aspect ratio 16:9. Title at top center in small caps, exactly: "MC-WM: residual world model with LLM-guided symbolic features for sim-to-real MBRL". The figure is organized as four acts flowing left to right, each act in a framed region with a small Roman numeral label (I / II / III / IV) and a one-line subtitle underneath.

Act I (leftmost 22%, titled "problem"): two side-by-side minimalist stick-figure locomotion robots, drawn as line-art HalfCheetah and Walker silhouettes on a shared horizontal baseline. The left robot is tinted pale teal on a smooth floor with a short gravity arrow; label "sim env, M_sim". The right robot is tinted pale orange on a hatched rough floor with a taller gravity arrow and a small mass symbol; label "real env, M_real". Between them a bold red ≠ symbol with caption "dynamics gap".

Act II (center-left 24%, titled "insight: additive residual"): centered large equation "M_real = M_sim + δ_θ" with δ_θ boxed in orange. Below the equation, δ_θ expands into two stacked sub-blocks: top block labeled "SINDy basis Φ(s,a)" with example terms "{s, a, s², sin(q)}" in teal; bottom block labeled "NAU head" in smaller teal font. A short arrow from "paired real transitions (s,a,s')" enters δ_θ from below. A tiny inset with caption "LLM proposes new Φ online".

Act III (center-right 30%, titled "pipeline: trust-filtered imagination"): a left-to-right flow: corrected model M_sim + δ_θ → imagined rollouts as five thin curved trajectory lines entering a funnel. The funnel has three stacked filter rings. Ring 1 labeled "Bellman Q^Δ trust weight". Ring 2 labeled "constraint system". Ring 3 labeled "Pareto HP gate". At each ring, one or two trajectories peel downward into a muted red "rejected" dustbin. Surviving trajectories exit to "model buffer" → "SAC update" with a minimal 2-layer MLP icon. Over the top of Act III, a thin banded region spanning Acts II–III labeled "Claude LLM orchestrator" with four dashed downward arrows landing on: "Role 2: feature proposal" (into SINDy), "Role 1: constraints", "Role 3: audit", "Role 5: HP tune". The orchestrator itself is drawn as three abstract overlapping neutral circles.

Act IV (rightmost 24%, titled "theory & applicability"): stacked vertically. Top half: a small 2D state-action support diagram — an olive blob labeled "supp(d^π_data)" with a red dot outside labeled "OOS" and a teal dot inside labeled "in-support"; overlay the formula "‖V_real − V_resid‖ ≤ 2γR/(1−γ)² · TV_supp". Bottom half: two side-by-side horizontal strips. Left pale green with a green check, label "physics gap → MC-WM works, 9.6–13× over sim"; right pale red with a red cross, label "observation / actuator gap → method underperforms". Caption under both: "residual correction is consistent only on the data-collection policy's support".

Whitespace between acts; thin vertical dividers; clean equations; 6pt minimum label readability; balanced left-to-right composition; only the labels specified above — no hallucinated text.
