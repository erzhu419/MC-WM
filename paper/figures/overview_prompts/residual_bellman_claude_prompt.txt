Create a single horizontal 16:9 illustration for a NeurIPS/ICLR paper, pure vector-flat scientific style, white background, thin 0.6pt black outlines, sans-serif labels (Helvetica-like), charcoal #2b2b2b body text, palette strictly limited to deep teal #264653, muted orange #E76F51, sand #F4A261, olive #8DB580, pale gray #E9ECEF fills — no gradients, no 3D, no shadows, no photorealism, no cartoon, no corporate logos, no watermarks. Aspect ratio 16:9. Title at top center in small caps, exactly: "Bellman Q-delta critic: model-trust as a discounted Bellman value". The figure is organized as three acts flowing left to right inside framed regions, each with a small Roman numeral label (I / II / III) and a one-line subtitle underneath.

Act I (leftmost 30%, titled "per-step confidence"): a small horizontal panel showing one transition. On the left, a teal stick-figure HalfCheetah at state s, with action arrow a. The corrected world model M_sim + delta_theta predicts a next state s'_hat shown as a small orange dotted silhouette; the true real next state s' is shown as a teal solid silhouette right next to it. A short red bracket between them is labeled epsilon, with caption "residual error on this step". Below that, the formula c_step(s,a) = 1 / (1 + epsilon^2 / tau) is centered. To the right, a small inset y-axis from 0 to 1 with the same formula plotted as a smooth decreasing curve from 1 down to 0 as epsilon increases, with the y-axis labeled "c_step in (0,1]" and a thin arrow tagged "trust this step".

Act II (center 35%, titled "Bellman backup of trust"): a horizontal chain of three nodes representing successive states (s_t, a_t) -> (s_t+1, a_t+1) -> (s_t+2, a_t+2). Each node is a small olive circle. Above each node draw a small badge with the corresponding c_step value as a labeled box: c_t = 0.85 (green tint), c_t+1 = 0.30 (orange tint), c_t+2 = 0.95 (green tint). Between the nodes thin teal arrows. Below each node a small box labeled Q^delta(s, a). Connect those boxes with curved arrows running right-to-left, each labeled gamma to indicate Bellman bootstrapping. Centered below the chain: the equation Q^delta(s_t, a_t) = log c_step(s_t, a_t) + gamma * (1 - done) * Q^delta(s_t+1, a_t+1), written cleanly with delta as a subscript using the actual letter delta. Add a small caption: "future model errors discount the present trust".

Act III (rightmost 35%, titled "rollout-weight gate"): A short imagined rollout drawn as five thin curved trajectory lines entering a vertical funnel. The funnel has a single gate ring labeled "weight w = exp(Q^delta(s,a)) in (0, 1]". Two of the five trajectories peel downward into a muted red dustbin labeled "drop, w < 0.01". Three surviving trajectories exit the funnel to a small box "model buffer". Above the funnel place a small histogram-like sketch showing trust distribution: x-axis "w in (0,1]", three thin teal bars at 0.1, 0.4, 0.9 of differing heights, conveying typical empirical distribution. Add a small caption under the funnel: "rollout transitions reweighted by trust before SAC sees them".

Whitespace between acts; thin vertical dividers; clean equations using actual Greek letters delta, epsilon, gamma, tau; 6pt minimum label readability; balanced left-to-right composition; only the labels specified above — no hallucinated text. Do not invent legend entries, do not draw extra robots or extra equations beyond those described, do not duplicate symbols.
