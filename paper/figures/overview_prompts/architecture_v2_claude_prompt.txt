Create a single 1024x1024 horizontal-flow figure for a NeurIPS/ICLR paper, pure vector-flat scientific style, white background, thin 0.6pt black outlines, sans-serif labels (Helvetica-like), charcoal #2b2b2b body text, palette strictly limited to deep teal #264653, muted orange #E76F51, sand #F4A261, olive #8DB580, pale gray #E9ECEF fills — no gradients, no 3D, no shadows, no photorealism, no cartoons, no logos, no watermarks. Title at top center in small caps, exactly: "MC-WM ARCHITECTURE: residual world model + LLM-guided trust filter + safe MBPO". One-line subtitle below in smaller font: "M_real ≈ M_sim + δ_θ; rollouts trust-filtered before SAC".

Layout is a left-to-right pipeline organized in three vertical bands.

Band A (left 28%, "data and base model"): two stacked horizontal panels. Top panel pale teal: a small line-art HalfCheetah silhouette on a smooth floor, label "sim env M_sim", with small caption "200k transitions". Bottom panel pale orange: same silhouette on a hatched rougher floor, label "real env M_real", caption "50k transitions". A short rightward arrow from each panel labeled "data" feeds into a single olive box labeled "ensemble world model M_sim (K=5)" centered between the panels at the band boundary.

Band B (center 36%, "residual learning"): centered large equation "M_real = M_sim + δ_θ" with the δ_θ symbol boxed in muted orange. Below the equation, the δ_θ box expands downward into two stacked sub-blocks: top in deep teal labeled "SINDy basis Φ(s,a)" with sample tokens "{s, a, s², sin(q), x_i x_j x_k, llm-proposed}"; bottom in olive labeled "NAU/NMU head with bounded L_eff". A thin upward dashed arrow from below enters the SINDy block, labeled "real-env paired transitions". A thin labeled callout to the right of δ_θ reads "Claude/GLM Role #2 proposes new Φ online; orthogonal expansion adds 3-way crosses".

Band C (right 36%, "trust-filtered rollouts → SAC"): the corrected-model output M_sim+δ_θ is shown as a single rectangle in sand yellow on the left of this band; from it five thin curved trajectory lines fan rightward into a horizontal funnel. The funnel has two stacked filter rings drawn as concentric capsules. Ring 1 in deep teal labeled "Bellman Q^δ trust weight w=exp Q^δ", ring 2 in muted orange labeled "constraint mask m (physics + LLM Role #1)". Two of the five trajectories peel downward into a small muted-red dustbin labeled "drop, w·m < 0.01". Three surviving trajectories exit the funnel into a rounded olive rectangle labeled "model buffer". From the model buffer a single right-pointing arrow leads to a 2-layer minimal MLP icon labeled "RE-SAC (LCB ensemble)". A thin curved feedback arrow from the SAC icon back to the M_real panel in Band A is labeled "online rollouts every k_real steps".

Across the top above all three bands draw a thin pale-gray banner labeled "LLM orchestrator (Claude / GLM-5.1)" with four short downward dashed arrows landing on: Role #1 constraints (Band C ring 2), Role #2 features (Band B SINDy block), Role #3 audit (Band B NAU block), Role #5 HP-Pareto gate (Band C funnel exit). The orchestrator banner contains three small overlapping neutral circles to suggest multi-role.

Whitespace between bands; thin vertical dividers; clean equations using actual Greek letters δ, φ; 6pt minimum label readability; balanced left-to-right composition; only the labels specified above — no hallucinated text, no extra legends, no duplicated symbols.
