Create a 1024×1024 publication-quality vector-flat scientific figure on a white background, using only deep teal #264653, muted orange #E76F51, sand #F4A261, olive #8DB580, pale gray #E9ECEF, and charcoal #2b2b2b text, with Helvetica-like sans-serif typography, no 3D, no shadows, no photorealism, no cartoons, no logos. Use a radial “discounted trust orbit” composition: in the center place a large deep-teal node labeled “(s,a)” surrounded by a circular Bellman critic ring labeled “Q^δ(s,a)”, with a clockwise rollout spiral of smaller nodes labeled “(s′,a′)”, “(s₂,a₂)”, and “future”, where each node has a small ε error bar and a trust chip labeled “c_step”. At upper left, draw a compact corrected-world-model module feeding into the central node, with only these equations: “M_real ≈ M_sim + δ_θ”, “ε = ‖real(s,a) − corrected(s,a)‖”, and “c_step(s,a)=1/(1+ε²/τ)”. Along the spiral, draw curved muted-orange back-up arrows from future nodes back toward the center, annotated with γ, γ², and γ³, visually showing future model errors discounting present trust like a value function. On the right side, place a clean equation panel in pale gray containing exactly “Q^δ(s,a)=log c_step(s,a)+γ(1−done)Q^δ(s′,a′)” and beneath it “w(s,a)=exp Q^δ(s,a) ∈ (0,1]”. At the bottom, show a split gate: a muted-orange downward branch labeled “w < 0.01: drop” ending in a small discard icon, and an olive branch labeled “rescale + push” leading to a rounded rectangle labeled “model buffer → SAC”. Add a concise central caption under the ring: “present trust is discounted by future model errors”. Ensure all arrows, nodes, and labels are crisp and evenly spaced, with no extra labels, no legend, and no text other than the exact labels and equations specified here.
