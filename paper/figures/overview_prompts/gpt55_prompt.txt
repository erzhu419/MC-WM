Create a single 16:9 horizontal NeurIPS-style paper overview figure on a white background, clean vector-flat scientific style, thin black outlines, neutral palette using deep teal #264653, muted orange #E76F51, sand #F4A261, olive #8DB580, charcoal text; no photorealism, no 3D render, no cartoon, no watermark. Title at top center in small caps, exact text: "MC-WM: residual world model with LLM-guided symbolic features". Layout: three main panels left-to-right plus an upper orchestration band and a bottom strip. LEFT panel: show sim-to-real physical-parameter gap with two side-by-side minimal line-art locomotion robots, a simulated HalfCheetah/Walker labeled "sim env M_sim" on a smooth pale grid floor with short gravity arrow and low-friction smooth texture, and a real robot labeled "real env M_real" on rough textured floor with longer gravity arrow, heavier mass blocks, friction hatching; place a bold gap symbol "≠ / gap" between them, with small icons for gravity, friction, mass. CENTER panel: show residual decomposition with large equation "M_real = M_sim + δ_θ"; depict δ_θ as an orange highlighted symbolic residual box containing "SINDy basis Φ(s,a): {s, a, s², sin(q), q̇a, …}" feeding into a compact teal "NAU neural head" block, with highlighted symbolic terms and arrows; below it show small data-flow "paired real transitions (s,a,s′) → residual fit Δs = s′ − M_sim(s,a)" with dots/mini trajectories entering the residual box. UPPER band spanning above the pipeline: draw a stylized abstract AI agent labeled "Claude LLM orchestrator" as a geometric node cloud / small neutral agent icon, not any corporate logo; four thin arrows descend from it to stages labeled exactly "feature proposal" into the SINDy basis, "constraint proposal" into the constraint filter, "audit" into rejected rollouts, and "hyperparameter tuning" into the Pareto gate. RIGHT panel: show a safety/trust funnel with decreasing width and rollouts visibly discarded at each stage: "imagined rollouts from corrected world model" entering from left as many curved trajectory lines, then a funnel section labeled "Bellman Q^Δ trust-weight filter", then "constraint system: physical + LLM-proposed predicates", then "filtered model buffer", then arrow to "SAC policy update" with a small policy network icon; rejected rollouts peel downward into a muted red/orange reject bin labeled "rejected rollouts", and the funnel geometry clearly removes some trajectories at each filter. Include a small "Pareto gate" near hyperparameter tuning before/above the trust filter. BOTTOM strip across full width labeled "Applicability Regime": left half tinted pale green with large green check and icons/text "physics-dynamics gaps: gravity, friction, mass"; right half tinted pale red with large red cross and icons/text "observation-scale / broken-actuator gaps"; centered caption along the strip, exact text: "residual correction is only consistent on the data-collection policy's support". Ensure all labels are crisp sans-serif, charcoal, readable at 6pt when rendered page-width, with balanced whitespace, conference-paper system-figure polish, and no extraneous text beyond specified labels.
